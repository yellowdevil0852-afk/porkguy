# 數值與邏輯總覽

> 這份檔案專門放**遊戲裡所有數據、公式、設計邏輯**，方便日後查資料或調數值。
> 「現在還沒做完的事、正在做什麼、遇到什麼問題」記在 [CLAUDE.md](CLAUDE.md)，
> 這裡只放「這個系統目前（或設計中）長怎樣」，不放待辦狀態。
> 已經上線的數值標「**現況**」，還沒實作、只是設計中的標「**設計中**」。

## 目錄

1. [地形](#地形)
2. [職業](#職業)
3. [怪物](#怪物)
4. [等級與成長](#等級與成長)
5. [戰鬥公式](#戰鬥公式)
6. [裝備（現況）](#裝備現況)
7. [裝備（浮動點數分配，設計中）](#裝備浮動點數分配設計中)
8. [特效詞綴 AFFIX（設計中的新規則）](#特效詞綴-affix設計中的新規則)
9. [技能](#技能)
10. [競技場](#競技場)
11. [商店與經濟](#商店與經濟)
12. [地圖與王座](#地圖與王座)

---

## 地形

`src/data.js` 的 `TER`，`cost` 是移動消耗，`atk`/`def` 是站在上面的加成。

| 代號 | 名稱 | 消耗 | 防禦 | 攻擊 | 高度 | 特殊 |
|---|---|---|---|---|---|---|
| R | 道路 | 1 | 0 | 0 | 0.00 | |
| P | 平原 | 1 | 0 | 0 | 0.00 | |
| F | 森林 | 2 | +2 | 0 | 0.10 | `forest`（疾風步職業走進去消耗算 1） |
| M | 山地 | 2 | +2 | +1 | 0.60 | `high`（高地，見戰鬥公式的高低差加成） |
| S | 沼澤 | 3 | −1 | 0 | −0.10 | `mire`（踏進去移動力立刻歸零） |
| W | 深水 | 99 | 0 | 0 | −0.34 | 不可通行 |
| T | 王座 | 1 | +2 | 0 | 0.30 | 勝利點 |
| C | 營地 | 1 | +2 | 0 | 0.10 | |
| K | 石頭 | 99 | 0 | 0 | 0.45 | `block`（不可通行，擋遠程/魔法視線；主地圖和競技場都會生成） |

視線公式（`hasLOS`/`canReach`，`src/rules.js`）：兩點連線密集取樣，
中間只要有一個取樣點落在 `block` 地形就算擋住；距離 ≤1（貼身）永遠有視線。

裝飾模型（`buildWorld()`，`src/world.js`）：山地跟石頭障礙以前共用同一顆
`rock_c`，長得一模一樣分不出來，現在山地改用 `rock_a`（之前完全沒用到的
第三種石頭素材，形狀比較尖、比較大顆）疊一顆主峰、配一兩棵樹裝飾成
「有樹的山」；石頭障礙維持原本 `rock_c`/`rock_b`「矮胖一叢好幾顆、
光禿禿」的樣子，兩者輪廓差夠多，一眼看得出哪個能走哪個不能走。

---

## 職業

`src/data.js` 的 `CLS`，五選三開局。`healPct`／被動描述裡的百分比都是
攻擊力的百分比或固定值，寫在各自的 `pass` 欄。

| 代號 | 名稱 | 進階(Lv5) | HP | 攻 | 防 | 移 | 射 | 傷害/護甲 | 被動 |
|---|---|---|---|---|---|---|---|---|---|
| KN | 騎士 | 聖殿騎士 | 34 | 11 | 7 | 4 | 1 | 斬/重甲 | 守護：周圍一格友軍減傷 −2 |
| RG | 遊俠 | 神射手 | 24 | 10 | 3 | 5 | 3 | 射/輕甲 | 鷹眼：站山地射程 +1 |
| MG | 法師 | 大賢者 | 20 | 13 | 2 | 4 | 3 | 法/布甲 | 穿透：無視目標一半防禦 |
| CL | 牧師 | 主教 | 24 | 8 | 4 | 5 | 3 | 法/輕甲 | 祝福：治療自己也回血 30%；`healPct:1.5` |
| BB | 狂戰士 | 狂暴領主 | 30 | 14 | 4 | 4 | 1 | 斬/輕甲 | 嗜血：生命每少 10% 攻擊 +1 |

剋制三角（`BEATS`）：法剋重甲、斬剋布甲、射剋輕甲，中了 ×`COUNTER_TRI`（見下）。

Lv5 進階（`PROMO_LV`/`PROMO`）：`hp+12 atk+4 def+3 mov+1`，改名不改代號。

---

## 怪物

`src/data.js` 的 `MON`，怪物整體比英雄弱、靠數量和地利取勝。

| 代號 | 名稱 | HP | 攻 | 防 | 移 | 射 | 傷害/護甲 | 經驗 | 體型 | 掉落率 |
|---|---|---|---|---|---|---|---|---|---|---|
| minion | 骷髏小兵 | 14 | 6 | 1 | 4 | 1 | 斬/輕甲 | 30 | 0.9 | 0.18 |
| warrior | 骷髏戰士 | 26 | 9 | 5 | 4 | 1 | 斬/重甲 | 55 | 1.0 | 0.34 |
| rogue | 骷髏獵手 | 19 | 8 | 2 | 5 | 2 | 射/輕甲 | 50 | 1.0 | 0.30 |
| mage | 骷髏術士 | 17 | 10 | 1 | 4 | 3 | 法/布甲 | 60 | 1.0 | 0.36 |
| boss | 骨王 | 58 | 14 | 8 | 4 | 1 | 斬/重甲 | 200 | 1.45 | 1.0（金色調） |

### 營地等級（依離王座遠近，`placeCamps()` 四圈由外而內排，每種地圖大小都有）

怪物等級不再是固定表，改成**跟地圖大小掛鉤**——地圖越大局越長，
角色也該練得越高，中心的最終守衛才不會變成站著的經驗值。
`monLvFor(tier)`（`data.js`）：四個 tier 各佔「這個地圖大小的最高等級」的
固定比例 10%/40%/70%/100%，最高等級本身查 `MON_MAXLV_BY_SIZE`：

| 地圖大小 | 最高等級 | tier1 (10%) | tier2 (40%) | tier3 (70%) | tier4 (100%) |
|---|---|---|---|---|---|
| 16 | 7 | 1 | 3 | 5 | 7 |
| 32 | 10 | 1 | 4 | 7 | 10 |
| 64 | 15 | 2 | 6 | 11 | 15 |
| 96 | 22 | 2 | 9 | 15 | 22 |
| 128 | 30 | 3 | 12 | 21 | 30 |

各圈陣容（`newGame()` 裡的 `kinds`，不分地圖大小）：

| 圈 tier | 陣容 |
|---|---|
| 1（最外圈） | 小兵×2、獵手×1 |
| 2 | 戰士、獵手、術士、小兵 |
| 3 | 骨王、戰士、術士、小兵 |
| 4（王座最終守衛） | 骨王×2、術士、戰士 |

每圈的營地數量（`CAMP_RING_N`，四圈由外而內）也依地圖大小查表：

| 地圖大小 | 圈1營地數 | 圈2 | 圈3 | 圈4 | 實測怪物總數 | 目標總數 |
|---|---|---|---|---|---|---|
| 16 | 4 | 2 | 2 | 2 | ~30 | 32 |
| 32 | 8 | 4 | 4 | 4 | ~60 | 64 |
| 64 | 30 | 14 | 14 | 14 | 258 | 256 |
| 96 | 64 | 32 | 32 | 32 | 576 | 576 |
| 128 | 118 | 56 | 56 | 56 | 1026 | 1024 |

64/96/128 這三個密度比較高，光靠角度算出來的落點常常撞到別的營地或地形，
`placeCamps()` 加了重試機制：落點放不下就沿著**同一個角度**（不是隨機亂跳，
維持鏡射對稱）往外或往內挪幾格再試幾次，撞了才放棄。這個機制上線後
placement 成功率幾乎 100%，所以這幾個數字已經很接近精算值，不是像
16/32 那樣只是抓個大概。

怪物等級成長 `MON_GAIN = {hp:4, atk:1, def:1}`（刻意比英雄的 `LV_GAIN` 平緩）。

重生：`MON_REVIVE_BASE=5` 回合起跳，等級每高一級 +1 回合；
王座半徑 `NO_REVIVE_R=5` 格內的營地清掉後**不會再重生**。

### 精英變種（`MON_ELITE`，現況）

任何非首領怪物有 **15%** 機率（`chance`）生成成精英：

| 項目 | 倍率/加成 |
|---|---|
| HP | ×1.4 |
| 攻擊 | ×1.25 |
| 防禦 | ×1.15 |
| 擊殺經驗 | ×1.8 |
| 擊殺金幣 | ×1.6 |
| 掉落機率 | 裝備 25%→50%、技能書 15%→30% |
| 外觀 | 換色（`0xd68cff` 紫）+ 放大 1.15 倍，名字前加「精英」 |

---

## 等級與成長

- 封頂 `LV_MAX=10`。升級所需經驗 `XP_NEED(lv) = 55 + (lv-1)*25`。
- 經驗來源：命中 `XP_HIT=12`、擊殺 `XP_KILL_PC=55`（怪物用 `MON.exp`）、
  治療 `XP_HEAL=14`、反擊 `XP_CNT=8`。
- 分享：`XP_SHARE=0.4`（40%），`XP_SHARE_R=3` 格內隊友都能分到。
- 每級固定成長 `LV_GAIN = {hp:5, atk:2, def:1}`，另外 `FREE_PTS=2` 點
  自由分配，換算 `PT_GAIN = {atk:1, def:1, hp:4}`（1 點攻=1 點防=4 點命，
  **裝備浮動分配的換算比例就是照這個抄的**，見後面章節）。
- Lv5 進階見「職業」章節的 `PROMO`。
- 站在自家營地周圍一格：每回合開局回復 `CAMP_HEAL=25%` 最大生命。
- 倒下：`REVIVE_TURNS=3` 回合後在營地復活，生命回一半、損失三成經驗，
  復活後 `REVIVE_DASH=2` 回合額外 +2 移動。

---

## 戰鬥公式

```
攻擊值 A = 攻擊力(atkOf) + 地形攻擊加成
  高地/低地：站高地打非高地 +HIGH_GROUND(2)；反過來 −1
防禦值 D = 防禦力(defOf) + 地形防禦 + 護盾值(guardOf)
  法師被動「穿透」或 rend 詞綴：D 直接砍半（Math.floor(D/2)）
基礎傷害 v = A × MIT_K / (MIT_K + D)     ← MIT_K = 18，減傷曲線常數
```

減傷曲線取代舊的「攻擊−防禦」線性公式：防禦永遠有效但不會把傷害壓到 0，
`MIT_K=18` 代表「防禦=18 時傷害剛好減半」。

再依序乘上這些修正（都是連乘，不是相加）：

| 條件 | 倍率/加成 |
|---|---|
| 剋制三角命中（傷害類型剋護甲類型） | ×`COUNTER_TRI` = **1.2** |
| 側面攻擊 | ×`FLANK.side` = **1.25** |
| 背後攻擊（且不會被反擊） | ×`FLANK.back` = **1.25** |
| 暴擊（機率 `CRIT=10%`） | ×`CRIT_MULT` = **1.5** |
| 蓄力（競技場一次性增益，用掉即消耗） | ×**1.5**（原本 ×2，已調降） |
| 其他技能自帶倍率（`opt.mult`） | 技能各自定義，見 `skills.js` |

最終 `v = Math.round(v)`，且**最少 1 點傷害**（`Math.max(1, v)`）。

其他戰鬥規則：
- 反擊條件（`canCounter`）：防守方存活、在攻擊方射程內、沒被背擊、
  攻擊方沒有「先制」效果、雙方之間有視線（`canReach`，見地形章節）。
- 反擊傷害額外乘 `pasOf(a,'counterPct')`（例如遊俠系技能「反擊大師」+50%）。
- 遠程抗性 `pasOf(d,'rangedRes')`：非近戰傷害額外打折。

---

## 裝備（現況）

裝備分三部位（武器/防具/飾品），品質 `RARITY` 五階：

| 品質 index | 名稱 | 倍率 mult | 顏色 |
|---|---|---|---|
| 0 | 普通 | 0.6 | `#c9d2e0` |
| 1 | 常見 | 1.0 | `#5fd39a` |
| 2 | 稀有 | 1.4 | `#5aa2ff` |
| 3 | 史詩 | 2.0 | `#c07dff` |
| 4 | 傳說 | 2.5 | `#e8c168` |

**現況掉落邏輯**（`rollItem()`，`src/rules.js`）：每個屬性的固定基礎值
（見下表）乘上品質倍率、四捨五入，正值最少取 1。**這套即將被下面
「裝備浮動點數分配」取代**，取代前先列出現況當對照。

| 部位 | 型 | 固定基礎值 | 職業限制 |
|---|---|---|---|
| 武器 | 短劍 | atk 3 | 全部 |
| 武器 | 闊劍 | atk 5 | KN, BB |
| 武器 | 戰斧 | atk 6, def −1 | BB, KN |
| 武器 | 長弓 | atk 4, rng 1 | RG |
| 武器 | 法杖 | atk 5 | MG, CL |
| 武器 | 權杖 | atk 3, hp 6 | CL, MG |
| 防具 | 皮甲 | def 3 | 不限 |
| 防具 | 鎖子甲 | def 5 | 不限 |
| 防具 | 板甲 | def 7, mov −1 | 不限 |
| 防具 | 法袍 | def 2, hp 8 | 不限 |
| 防具 | 鬥篷 | def 3, mov 1 | 不限 |
| 飾品 | 力量護符 | atk 3 | 不限 |
| 飾品 | 守護石 | def 3 | 不限 |
| 飾品 | 生命寶珠 | hp 12 | 不限 |
| 飾品 | 疾行靴 | mov 2 | 不限 |
| 飾品 | 鷹眼徽章 | rng 1 | 不限 |
| 飾品 | 戰旗 | atk 2, def 2 | 不限 |

掉落品質分布見「商店與經濟」章節的 `Q_TABLE`。

---

## 裝備（浮動點數分配，設計中）

> **狀態：設計已大致定案，還沒寫程式。** 開工前重讀一次這節，
> 有標「⚠️ 待確認」的地方要先跟使用者對過。

### 為什麼要改

現況的固定值乘品質倍率，在傳說品質會被放大到誇張（例：板甲 def:7
在傳說變成 def:18，剛好等於 `MIT_K`，一件裝備直接把傷害砍半）。
改成「先定總點數預算、再隨機分配到各屬性」，同品質同部位的裝備會
長得不一樣（多樣性），也比較好從點數總量控制強度上限。

### 點數預算表（草案，數字本身待實測調整）

| 品質 | 武器 | 防具 | 飾品 |
|---|---|---|---|
| 普通 | 4 | 4 | 3 |
| 常見 | 6 | 6 | 5 |
| 稀有 | 8 | 8 | 7 |
| 史詩 | 11 | 11 | 9 |
| 傳說 | 13 | 13 | 11 |

武器/防具共用同一條曲線（用「白板武器=4、常見護甲=6」兩個錨點，
沿品質倍率曲線內插出來的）；飾品因為彈性最大，預算故意抓低一點。

### 部位規則

- **武器**：只能抽 `攻擊力`、`生命`、`移動`。保底 `攻擊力 ≥ 總點數 40%`
  （`Math.ceil(P*0.4)`），扣掉保底後剩下的點數：
  - **移動已解鎖（史詩/傳說）**：每點 **40% 攻擊力／40% 生命／10% 移動／
    10% 什麼都不加（浪費掉）**——這四個機率是照字面上的數字，不是
    重新歸一化過的相對權重（已跟使用者確認過）。
  - **移動未解鎖（普通/常見/稀有）**：每點 **45% 攻擊力／45% 生命／
    10% 什麼都不加**（已跟使用者確認）。
- **防具**：只能抽 `生命`、`防禦`、`移動`。保底 `生命 ≥ 總點數 60%`
  （`Math.ceil(P*0.6)`），剩下的點數在生命/防禦/移動之間分配——
  ⚠️ 待確認：這裡的機率使用者還沒像武器那樣明講是不是也要「留一部分
  浪費掉」，目前先假設是均分（1/3 各一份），跟武器那套不一致，
  動工前要問清楚防具是不是也要套「留 X% 浪費」的邏輯。
- **飾品**：`攻擊力`、`防禦`、`生命`、`移動`、`射程`全部都能抽，
  **完全隨機、沒有下限、不限制分到幾種屬性**（可能五種都各分到一點）。

### 移動／射程的額外限制

- 只有**史詩、傳說**品質才可能抽到移動或射程，普通/常見/稀有完全排除
  這兩個屬性（不管哪個部位）。
- 換算比例：**4 點 = 1 移動、5 點 = 1 射程**。
- 硬上限：**移動最多 +2、射程最多 +1**，算出來超過就直接夾住，
  多分配的點數不退回去補其他屬性（刻意選簡單版本）。
- 換算成整數用四捨五入，**不強制最少 1**（跟攻擊/防禦/生命不一樣，
  這兩個允許「有分配到點但四捨五入變 0」的撲空結果）。

### 換算比例總表

| 屬性 | 1 點換算 | 上限 | 依據 |
|---|---|---|---|
| 攻擊力 | 1 | 無 | 沿用 `PT_GAIN.atk` |
| 防禦 | 1 | 無 | 沿用 `PT_GAIN.def` |
| 生命 | 4 | 無 | 沿用 `PT_GAIN.hp` |
| 移動 | 4 點/1移動 | +2 | 使用者指定 |
| 射程 | 5 點/1射程 | +1 | 使用者指定 |

### 武器的「型」保留

短劍/闊劍/戰斧/長弓/法杖/權杖……這些型不變，型只決定名字/外觀/
職業限制，浮動分配只改「型底下的數值」。

### 舉例：史詩武器（P=11）

`floor = Math.ceil(11*0.4) = 5` 點保底進攻擊力，`remain = 6` 點，
每一點獨立擲骰：40% 攻擊力、40% 生命、10% 移動、10% 浪費。
6 點是多項分布（4 個分類），不逐一列表——重點是「浪費」這個結果
是真實存在的分類，不是拿去補別的屬性。

### 待實作時要改的檔案

- `rollItem()`、`itemStats()`、`itemName()`（`src/rules.js`）：
  現況讀 `GEAR` 固定表的地方要改成讀點數分配後的結果。
- `GEAR` 表（`src/data.js`）：武器/防具/飾品的「型」清單留著（名字、
  職業限制），拿掉裡面的固定數值。

---

## 特效詞綴 AFFIX（設計中的新規則）

> **狀態：規則已定案，還沒寫程式。**

### 現況（即將被取代，列出來對照）

`AFFIX` 表（`src/data.js`）6 個詞綴，效果是固定值，不分品質：

| id | 名稱 | 效果 |
|---|---|---|
| vamp | 吸血 | 造成傷害時回復 30% |
| rend | 破甲 | 無視目標一半防禦 |
| first | 先制 | 主動攻擊不會被反擊 |
| thorn | 反傷 | 被近戰攻擊反彈 25% 傷害 |
| swift | 疾風 | 移動力 +2 |
| guard | 守護 | 受到的傷害 −2 |

附加機率（`rollItem()`）：`0.15 + 品質index×0.22`，只有品質>0 才判定，
中了之後 6 選 1 均分機率，跟部位無關。

### 新規則：數值按品質分級

| id | 名稱 | 常見 | 稀有 | 史詩 | 傳說 |
|---|---|---|---|---|---|
| vamp | 吸血 | 回復 2% | 4% | 6% | 10% |
| rend | 破甲 | 無視 5% 防禦 | 10% | 15% | 20% |
| thorn | 反傷 | 反彈 10% | 15% | 20% | 30% |
| guard | 守護 | −2 | −4 | −6 | −10 |
| first | 先制 | ✗ 抽不到 | ✗ 抽不到 | 有 | 有 |
| swift | 疾風 | ✗ 抽不到 | ✗ 抽不到 | +1 移動 | +2 移動 |

`rend` 改成分級百分比後，要跟法師被動「穿透」（固定無視一半防禦）
拆開算，兩個不能再共用同一行程式碼。

### 新規則：部位限制

- **只在武器上**：vamp、rend、first
- **只在防具上**：thorn、guard
- **任何部位（含飾品）都適用**：swift
- **飾品額外規則：飾品可以抽到全部 6 種詞綴**，不受上面部位限制
  （這是使用者特別確認過的——一開始漏寫這條，飾品差點變成只能抽
  swift，已經修正）。

### 抽取邏輯

沿用附加機率公式 `0.15+品質index×0.22`（品質>0 才判定）。判定中了之後，
候選池 = 這個部位能用的詞綴（武器→武器限定+任何部位；防具→防具限定+
任何部位；飾品→全部 6 個）裡面，**品質達到該詞綴最低品質**的項目，
在候選池裡均分機率抽一個（池子只剩一個選項就是 100% 抽中它）。

---

## 技能

`src/skills.js`，共 **90 個**：5 職業 × 15（每品質 3 個，另外被動技能
4 個分散在綠/藍/紫/金）+ 15 個通用技能。數值全部是「攻擊力的百分比」，
等級和裝備一起帶動成長。多目標技能照「實際常打到幾個」配預算
（直線抓 2、範圍抓 2.5），白色直線技能每發只有 90% 輸出，避免低品質
技能總輸出壓過高品質。

品質分布：0白/1綠/2藍/3紫/4金 各 18 個，五個品質平均分配。

取得：打怪 15% 掉技能書，寶箱也會掉（金寶箱必掉）。技能欄：主動槽
Lv1/3/6 各開一個，被動槽 Lv4 開。分解技能書換精球，三顆合成高一階
（`craftItem`，見 `src/inv.js`）。

---

## 競技場

`src/arena.js`：

- 觸發頻率 `ARENA_INTERVAL = 5`（每 5 回合結束強制觸發一次）。
- 場地 `ARENA_SIZE = 16`（16×16 獨立小地圖）。
- **單場打到某一方全滅為止就結束**（沒有三戰制）。
- 進場轉場動畫（`arenaBanner()`）：獨立一套，不是共用的 `showBanner()`。
  灰階模糊淡入＋字放大（0.7 秒）→ 字左右搖晃像船一樣，維持到第 3 秒 →
  灰色背景用 `radial-gradient` 配可動畫的 `--r` 自訂屬性從外圈往中心收
  （2 秒，效果是畫面邊緣先恢復、中心最後恢復），**字跟背景同時開始收、
  同時消失完**（不是背景先退、字晚一點才跟著滑走）。
- 地形每次用 `mulberry32(G.seed ^ G.turn×0x1000193)` 重新隨機生成，
  雙方算出來的結果保證一致：
  - 石頭（`K`）障礙：4～7 叢，純掩體、擋視線、無加成。
  - 地形變化：森林/山地/沼澤隨機灑在剩下的平原格上，帶原本的地形加成。
- 四種一次性增益地塊（`ARENA_BUFF_KINDS`），每次進場保證四種都出現一個
  （最小間隔 3 格避免擠在一起）：

| 種類 | 效果 |
|---|---|
| power（力量增幅） | 攻擊 +20%，3 回合 |
| charge（蓄力） | 下一次普通攻擊 +50% 傷害，打出去就消耗 |
| guard（守護） | 護盾 1.5 倍生命，3 回合 |
| haste（迅捷） | 移動 +2，2 回合 |

- 獎勵：贏家 `ARENA_GOLD_WIN=40` 金、輸家 `ARENA_GOLD_LOSE=10` 金；
  結束後所有參賽者回到主戰場原本位置，**血量統一恢復到上限的一半**。

---

## 商店與經濟

`src/shop.js`：

- 觸發頻率 `SHOP_INTERVAL = 3`（每 3 回合結束開一次）。
- 每次 5 件商品由低到高依序拍賣，其中保底一件金色（`Q_TABLE.gold`）。
- 拍賣**回合制**（不是即時搶標）：雙方輪流「加價」或「放棄」——
  放棄時對方已出價就直接得標，兩邊都沒出價就流標。
- 最低加價門檻 `SHOP_MIN_RAISE(q) = (q+1)×10`：白 10、綠 20、藍 30、
  紫 40、金 50，可以自己輸入更高。

### 金幣來源（`goldFromTier`，`src/inv.js`）

| 來源 | 金幣範圍 |
|---|---|
| 一般怪（1 級營地） | 2～4 |
| 一般怪（2 級營地） | 4～7 |
| 一般怪（3 級營地） | 6～10 |
| 首領 | 20～30 |
| 競技場勝/負 | 40 / 10（見上面競技場章節） |

精英變種擊殺金幣額外 ×1.6（見「怪物」章節）。

### 掉落品質分布（`Q_TABLE`，`rollQ()` 依權重擲）

權重是「普通/常見/稀有/史詩/傳說」五個數字，加起來 100：

| 表 | 普通 | 常見 | 稀有 | 史詩 | 傳說 | 用在哪 |
|---|---|---|---|---|---|---|
| `1`（1 級營地） | 45 | 35 | 16 | 4 | 0 | 一般小怪掉落 |
| `3`（3 級營地） | 25 | 33 | 27 | 13 | 2 | 中階怪掉落 |
| `5` | 10 | 22 | 32 | 26 | 10 | 高階/罕見來源 |
| `boss` | 0 | 10 | 30 | 40 | 20 | 首領掉落 |
| `gold` | 0 | 15 | 35 | 35 | 15 | 金寶箱、商店保底金色 |

裝備掉落機率 25%、技能書掉落機率 15%（各自獨立判定，精英怪雙倍）。

---

## 地圖與王座

- 地圖大小可選 `SIZES = [16, 32, 64, 96, 128]`，地形/道路/營地/寶箱按
  比例生成，永遠 180° 旋轉對稱。
- 王座勝利：`THRONE_WIN = 5`，站上王座連續守住 5 回合（每次輪到自己
  回合開始時計 1，王座上沒自己人就歸零）。
- 怪物驚醒距離 `AGGRO = 3`，離營地追擊上限 `LEASH = 8`。
- **石頭**（`placeRocks()`，`src/world.js`）：主地圖也會生成，不是只有
  競技場才有。叢數 `Math.max(3, Math.round(W*H/120))`，每叢有 40% 機率
  再黏一格，避開營地 3 格範圍內、避開已經是路/水/營地/王座的格子，
  只灑在還是平原的格子上，180° 鏡像對稱。`freeNear()` 本來就會跳過
  `cost>90` 的地形，所以怪物、英雄、寶箱都不會生成到石頭上，不用額外處理。
