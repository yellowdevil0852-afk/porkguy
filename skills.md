# 豬肉戰士 — 技能設計表

> 已經實作完畢。技能表在 `src/skills.js`、執行在 `src/rules.js` 的 `useSkill()`、
> 掉落與精球在 `src/inv.js`、特效在 `src/vfx.js`。數值調整直接改這裡再同步到 `src/skills.js`。

## 一、傷害預算尺

每個技能有一個「一次施放的期望總輸出」上限（攻擊力的百分比）。多目標技能**不是**照最大命中數除，
而是照實際常打到幾個除 —— 直線抓 2 個，範圍抓 2.5 個。

| 品質 | 期望總輸出 | 單體 | 直線每發 | 範圍每格 | CD |
|---|---|---|---|---|---|
| 白 普通 | 180% | 150% | 90% | 75% | 2–3 |
| 綠 常見 | 270% | 180% | 115% | 100% | 3 |
| 藍 稀有 | 380% | 210% | 145% | 130% | 3–4 |
| 紫 史詩 | 520% | 250% | 185% | 165% | 4–5 |
| 金 傳說 | 750% | 320% | 230% | 200% | 5–6 |

**單體技能吃不滿預算，剩下的換成狀態效果** —— 這是高品質單體技能的價值來源。
金色單體 320% 只比紫色多 70%，但額外帶一個控制，那才是差距。

> 舊版的問題：白色「貫穿射擊 110%×4 = 440%」比金色技能還猛，因為當時沒有多目標預算。
> 現在它是 90%×3（直線），期望輸出約 180%，回到白色該有的位置。

## 二、狀態表

所有數字都是施術者攻擊力的百分比。

### 增益（藍以上才會出現）

| 狀態 | 效果 |
|---|---|
| 恢復 | 每回合開始回復 X%，持續 N 回合 |
| 護盾 | 吸收 X 點傷害，最多存在 3 回合 |
| 強化 | 攻擊 +X% |
| 堅甲 | 防禦 +X% |
| 迅捷 | 移動 +X 格 |
| 銳利 | 暴擊率 +X% |

### 負面（紫以上）

| 狀態 | 效果 |
|---|---|
| 流血 | 每回合掉 X%；該單位移動時再掉一次 |
| 灼燒 | 每回合掉 X%；結束時傳染給相鄰一個敵人 |
| 劇毒 | 每回合掉 X%；受到的治療減半 |
| 虛弱 | 攻擊 −X% |
| 破甲 | 防禦 −X% |
| 詛咒 | 受到的所有傷害 +X% |

### 控制（金）

| 狀態 | 效果 |
|---|---|
| 暈眩 | 下回合不能移動也不能行動 |
| 定身 | 不能移動，可以行動 |
| 沉默 | 不能使用技能 |
| 冰凍 | 不能動，且受到的傷害 +50% |
| 恐懼 | 下回合強制遠離施術者，且不能攻擊 |

狀態只是工具，不是義務。純傷害、蓄力、位移、無視規則，都可以拿來拉開品質差距。

## 三、職業技能（每職業 15 個）

每品質 3 個。被動 4 個，分佈在綠／藍／紫／金各一個，**白色全是主動**。

### 騎士 KN（斬／重甲・前排）

| 品質 | 技能 | 類型 | 效果 |
|---|---|---|---|
| 白 | 猛擊 | 主動 | 單體 150%。CD 2 |
| 白 | 盾牆突擊 | 主動 | 單體 110%，攻擊後不會被反擊。CD 2 |
| 白 | 掃腿 | 主動 | 面向的三格扇形各 75%。CD 3 |
| 綠 | 十字斬 | 主動 | 上下左右四格各 100%。CD 3 |
| 綠 | 重擊 | 主動 | 單體 180%，擊退目標一格。CD 3 |
| 綠 | 重甲精通 | **被動** | 受到的遠程與法術傷害 −15% |
| 藍 | 聖裁 | 主動 | 單體 210%，命中後自身【堅甲 +40%，2 回合】。CD 4 |
| 藍 | 守護誓約 | 主動 | 周圍一格友軍各得【護盾 150%】，自己得【堅甲 +30%，2 回合】。CD 4 |
| 藍 | 鋼鐵之軀 | **被動** | 防禦 +15%，免疫擊退與拉扯 |
| 紫 | 破甲重擊 | 主動 | 單體 250%，附加【破甲 −35%，3 回合】。CD 4 |
| 紫 | 迴旋斬 | 主動 | 周圍一格所有敵人各 165%。CD 5 |
| 紫 | 復仇之盾 | **被動** | 每次被近戰攻擊，攻擊者附加【流血 20%，2 回合】 |
| 金 | 王者號令 | 主動 | 單體 320% 並【暈眩 1 回合】；周圍一格友軍【強化 +50%，2 回合】。CD 6 |
| 金 | 聖盾壁壘 | 主動 | 周圍一格所有敵人各 200% 並【定身 1 回合】。CD 6 |
| 金 | 不倒之軀 | **被動** | 生命歸零時保留 1 點並得【堅甲 +80%，1 回合】，每場一次 |

### 遊俠 RG（射／輕甲・遠程輸出）

| 品質 | 技能 | 類型 | 效果 |
|---|---|---|---|
| 白 | 快射 | 主動 | 單體 150%，不消耗移動。CD 2 |
| 白 | 貫穿射擊 | 主動 | 直線 3 格各 90%。CD 3 |
| 白 | 絆索 | 主動 | 3 格內空地放陷阱，踩到受 110% 並停止移動。CD 3 |
| 綠 | 雙連射 | 主動 | 對同一目標打兩次，各 115%。CD 3 |
| 綠 | 散射 | 主動 | 目標周圍一格內最多三個敵人各 100%。CD 3 |
| 綠 | 疾風步 | **被動** | 移動 +1，穿越森林不加成移動消耗 |
| 藍 | 鷹眼專注 | 主動 | 自身【銳利 +60%】+【強化 +30%】，2 回合。CD 4 |
| 藍 | 穿甲箭 | 主動 | 直線 4 格各 145%，無視地形防禦。CD 4 |
| 藍 | 獵人本能 | **被動** | 每回合第一次攻擊必定不被反擊 |
| 紫 | 毒箭 | 主動 | 單體 250%，附加【劇毒 25%，3 回合】。CD 4 |
| 紫 | 荊棘陷阱 | 主動 | 目標周圍一格放三個陷阱，各 185%，踩到附加【流血 25%，2 回合】。CD 5 |
| 紫 | 致命弱點 | **被動** | 對帶有負面狀態的目標傷害 +30% |
| 金 | 箭雨 | 主動 | 目標周圍一格各 200% 並【定身 1 回合】。CD 6 |
| 金 | 死亡標記 | 主動 | 單體 320%，附加【詛咒 +30%，2 回合】；目標本回合內死亡則冷卻立刻重置。CD 6 |
| 金 | 千里之瞳 | **被動** | 射程 +1（山地再 +1）；對 3 格外目標永不被反擊 |

### 法師 MG（法／布甲・純輸出，沒有治療）

| 品質 | 技能 | 類型 | 效果 |
|---|---|---|---|
| 白 | 奧術飛彈 | 主動 | 單體 150%，無視地形防禦。CD 2 |
| 白 | 火球 | 主動 | 目標周圍一格各 75%。CD 3 |
| 白 | 冰錐 | 主動 | 直線 3 格各 90%。CD 3 |
| 綠 | 烈焰噴發 | 主動 | 目標周圍一格各 100%。CD 3 |
| 綠 | 閃現 | 主動 | 瞬移到 5 格內空地。CD 5 |
| 綠 | 元素親和 | **被動** | 法術傷害 +10%，所有技能冷卻 −1 |
| 藍 | 海嘯 | 主動 | 3 格寬的橫向水牆各 145%，把命中的敵人隨機推開 1～2 格。CD 4 |
| 藍 | 秘法護盾 | 主動 | 自身【護盾 200%】+【堅甲 +50%，2 回合】。CD 4 |
| 藍 | 奧術洞察 | **被動** | 暴擊率 +15%，暴擊時對相鄰敵人濺射 20% |
| 紫 | 烈焰風暴 | 主動 | 目標周圍一格各 165%，附加【灼燒 25%，3 回合】。CD 4 |
| 紫 | 雷霆貫穿 | 主動 | 直線 5 格各 185%，附加【破甲 −25%，2 回合】。CD 5 |
| 紫 | 熵能迴響 | **被動** | 你的範圍傷害額外附加【虛弱 −20%，2 回合】 |
| 金 | 隕石術 | 主動 | 指定 5 格內一點，下回合開始落下，目標周圍兩格各 200%。CD 6（蓄力一回合，躲得開） |
| 金 | 絕對零度 | 主動 | 目標周圍一格各 200% 並【冰凍 2 回合】。CD 6 |
| 金 | 時間錯位 | **被動** | 每回合開始冷卻額外 −1；使用技能後 30% 不進冷卻 |

### 牧師 CL（法／輕甲・治療，形式多樣）

| 品質 | 技能 | 類型 | 效果 |
|---|---|---|---|
| 白 | 治癒之光 | 主動 | 單體治療 160%。CD 2 |
| 白 | 急救 | 主動 | 治療 90%，不消耗行動。CD 3 |
| 白 | 審判之光 | 主動 | 單體 130% 傷害。CD 3 |
| 綠 | 群體治療 | 主動 | 目標周圍一格友軍各治療 110%。CD 3 |
| 綠 | 治療光環 | 主動 | 自身 2 格內友軍每回合開始回復 25%，持續 4 回合。CD 5 |
| 綠 | 虔誠 | **被動** | 治療時自己回復該次治療量的 30% |
| 藍 | 加護 | 主動 | 友軍【護盾 190%】。CD 3 |
| 藍 | 神聖祝福 | 主動 | 友軍【恢復 45%／回合，3 回合】+【堅甲 +30%】。CD 4 |
| 藍 | 生命汲取 | **被動** | 你造成傷害時，生命最低的友軍回復該傷害的 50% |
| 紫 | 復甦禱言 | 主動 | 目標周圍一格友軍各治療 200%，並清除所有負面狀態。CD 5 |
| 紫 | 制裁 | 主動 | 單體 250% 傷害，附加【虛弱 −35%，3 回合】。CD 4 |
| 紫 | 生命鏈結 | **被動** | 你治療過的目標，下一回合受到的傷害 25% 轉到你身上 |
| 金 | 神聖領域 | 主動 | 自身 2 格內：友軍治療 290% 並【強化 +40%，2 回合】；敵人【沉默 2 回合】。CD 6 |
| 金 | 重生之息 | 主動 | 讓一個倒下的隊友立刻在你身邊站起來，生命回到 60%。CD 8，一場最多兩次 |
| 金 | 殉道 | **被動** | 你倒下時全隊回復最大生命 40% 並得【強化 +30%，2 回合】 |

### 狂戰士 BB（斬／輕甲・近戰爆發）

| 品質 | 技能 | 類型 | 效果 |
|---|---|---|---|
| 白 | 蠻力一擊 | 主動 | 單體 150%。CD 2 |
| 白 | 橫掃 | 主動 | 周圍一格所有敵人各 75%。CD 3 |
| 白 | 猛踏 | 主動 | 面前一格 110% 並擊退一格。CD 3 |
| 綠 | 衝鋒 | 主動 | 直線最多衝 4 格撞向敵人，180% 並擊退一格。CD 4 |
| 綠 | 旋風斬 | 主動 | 周圍一格所有敵人各 100%。CD 3 |
| 綠 | 嗜血 | **被動** | 生命每少 10%，攻擊 +1 |
| 藍 | 狂暴 | 主動 | 自身【強化 +60%，2 回合】+【迅捷 +2】。CD 5 |
| 藍 | 戰吼 | 主動 | 周圍兩格友軍【強化 +35%，2 回合】，自己再得【堅甲 +30%】。CD 5 |
| 藍 | 戰鬥狂熱 | **被動** | 每被攻擊一次，下次攻擊 +12%（最多五層，出手清空） |
| 紫 | 撕裂 | 主動 | 單體 250%，附加【流血 30%，3 回合】。CD 3 |
| 紫 | 血腥旋風 | 主動 | 周圍一格所有敵人各 165%，附加【流血 20%，2 回合】。CD 5 |
| 紫 | 血怒 | **被動** | 造成傷害的 30% 轉為生命；生命低於 50% 時翻倍 |
| 金 | 斷頭台 | 主動 | 單體 320%；目標生命低於 40% 時改為 500% 並【恐懼 2 回合】。CD 6 |
| 金 | 屠戮 | 主動 | 周圍一格所有敵人各 200% 並【暈眩 1 回合】。CD 6 |
| 金 | 不死狂戰 | **被動** | 每擊殺一個敵人立刻恢復本回合移動和行動（每回合最多兩次） |

## 四、通用技能（15 個）

分類：**近戰** = 騎士／狂戰士　**遠程** = 遊俠／法師　**輔助** = 牧師／法師　**全** = 五職業

| 品質 | 技能 | 類型 | 可學 | 效果 |
|---|---|---|---|---|
| 白 | 疾行 | 主動 | 全 | 立刻恢復本回合移動。CD 4 |
| 白 | 急救包 | 主動 | 全 | 治療自己或友軍 80%。CD 4 |
| 白 | 佯攻 | 主動 | 近戰 | 單體 110%；目標本回合的轉頭機會被消耗掉（更好繞背）。CD 3 |
| 綠 | 撤退 | 主動 | 全 | 往遠離敵人方向移動 3 格，不觸發反擊。CD 4 |
| 綠 | 集中射擊 | 主動 | 遠程 | 單體 180%，但本回合不能移動。CD 3 |
| 綠 | 老兵 | **被動** | 全 | 最大生命 +12% |
| 藍 | 戰術指揮 | 主動 | 輔助 | 一個友軍立刻恢復本回合的移動和行動。CD 5 |
| 藍 | 破陣 | 主動 | 近戰 | 單體 210%，無視目標所有地形加成。CD 4 |
| 藍 | 堅毅 | **被動** | 全 | 受到的持續傷害（流血／灼燒／劇毒）減半 |
| 紫 | 詛咒之觸 | 主動 | 遠程／輔助 | 單體 250%，附加【詛咒 +30%，3 回合】。CD 4 |
| 紫 | 破膽一擊 | 主動 | 近戰 | 單體 250%，附加【虛弱 −40%，2 回合】。CD 4 |
| 紫 | 反擊大師 | **被動** | 近戰 | 反擊傷害 +50%，且被背擊時仍可反擊 |
| 金 | 力場牢籠 | 主動 | 遠程／輔助 | 指定 3 格內一個敵人，它和周圍敵人【定身 2 回合】。CD 6 |
| 金 | 一騎當千 | 主動 | 近戰 | 本回合每擊殺一個敵人就恢復行動，並得【強化 +40%，1 回合】。CD 7 |
| 金 | 不屈意志 | **被動** | 全 | 免疫所有控制；受到的控制改為【堅甲 +30%，1 回合】 |

**總計 5 × 15 + 15 = 90 個技能。**

## 五、取得、槽位、分解

- **掉落進背包**，跟裝備同一個地方（背包的角色牌已經有技能欄，直接接上）
- **主動槽 Lv 1 / 3 / 6 解鎖，被動槽 Lv 4 解鎖**
- 任何技能可分解成 **1 顆該品質的技能精球**；**3 顆同品質精球 → 1 顆高一階精球**

### 掉落機率（每擊殺一隻怪，兩次獨立判定）

| 掉落 | 機率 |
|---|---|
| 裝備 | 25% |
| 技能書 | 15% |
| 兩件都掉 | 3.75% |
| 什麼都沒有 | 63.75% |

品質分佈照怪群等級（王座越近越好）：

| 來源 | 白 | 綠 | 藍 | 紫 | 金 |
|---|---|---|---|---|---|
| Lv1 外圈 | 45% | 35% | 16% | 4% | 0% |
| Lv3 中圈 | 25% | 33% | 27% | 13% | 2% |
| Lv5 王座圈 | 10% | 22% | 32% | 26% | 10% |
| 首領（骨王） | 0% | 10% | 30% | 40% | 20% |
| 金寶箱 | 0% | 15% | 35% | 35% | 15% |

## 六、裝備數值

一級的成長是 HP +5 ／ 攻 +2 ／ 防 +1。折成「點」配預算：

> 攻 +1 = 2 點　防 +1 = 2 點　HP +1 = 0.4 點　移 +1 = 4 點　射程 +1 = 8 點
> **1 級 = 8 點**

| 品質 | 級數 | 預算 | 武器 | 防具 | 飾品 |
|---|---|---|---|---|---|
| 白 | 0.6 | 4.8 點 | 攻 +2 | 防 +2 | HP +12 |
| 綠 | 1.0 | 8 點 | 攻 +4 | 防 +3、HP +5 | 移 +1、HP +10 |
| 藍 | 1.4 | 11 點 | 攻 +5、HP +5 | 防 +4、HP +8 | 攻 +2、防 +2、HP +8 |
| 紫 | 2.0 | 16 點 | 攻 +7、HP +5 | 防 +6、HP +10 | 移 +1、攻 +3、HP +10 |
| 金 | 2.5 | 20 點 | 攻 +8、射程 +1 | 防 +7、HP +15 | 移 +2、攻 +3、防 +3 |

三格全金 ≈ 7.5 級的屬性。詞綴照技能同一套規則：白綠無詞綴；藍一個屬性型；
紫一個特效型；金兩個或一個強力特效。

## 七、三件事的最終決定

1. **精球拿來做什麼？** —— <b>三顆同品質精球合成一個高一階的隨機技能</b>（只抽本隊職業用得到的）。
   分解得到同品質精球，所以爛掉落有出路，又不能直接指定金色。
2. **開局給什麼？** —— 每個角色開局送 1 個白色主動：
   騎士猛擊、遊俠貫穿射擊、法師火球、牧師治療術、狂戰士橫掃。
3. **技能有職業鎖嗎？** —— <b>鎖</b>。職業技能鎖死本職業，通用技能照可學欄。撿到用不上的拿去分解。

## 八、技能欄與操作

| 欄位 | 解鎖 |
|---|---|
| 主動 1 | Lv.1（開局就送一個） |
| 主動 2 | Lv.3 |
| 主動 3 | Lv.6 |
| 被動 | Lv.4 |

背包 → **技能**分頁：點角色牌上的技能欄選起來 → 點技能書裝上；**雙擊技能欄**卸下（技能書會退回背包）。
每本技能書右邊有**分解**鈕，換一顆同品質精球；分頁下面的精球列有各品質的**合成**鈕（要三顆）。


## 九、後來的調整（實作版）

- **隊伍改成五選三**，兩邊開局各自挑三個職業，可以撞職業。技能掉落只抽本隊職業用得到的。
- **裝備也有精球**：分解裝備得到同品質的**裝備精球**（跟技能精球分開算），一樣三顆合成一個高一階的裝備。
  背包裡有批量分解（白／白＋綠），設定裡也能開自動分解門檻，低品質掉落直接變精球不進背包。
- **地形移動消耗調降**：平原 1、森林 2、山地 2（防禦跟著從 +3 降成 +2）、沼澤 3、王座 1。
  腳程也跟著收：騎士 4、遊俠 5、法師 4、牧師 5、狂戰士 4。
- **經驗**：曲線 `55 + (lv-1)×25`；**三格內的隊友分 40% 經驗**，全隊一起成長。
  實測 AI 對戰下 Lv3 約第 3 回合、Lv4 約第 5 回合、Lv6 約第 10 回合、Lv10 約第 20 回合。
- **自由屬性點**：每升一級 2 點，在背包的角色卡上分配（攻擊 +1／防禦 +1／生命 +4）。
- **換裝限制**：**每個欄位**一回合只能換一次（武器／防具／飾品、三個主動技能欄、被動欄各自算），
  換過的欄位標 ✓。**卸下不算一次**（會把該格的次數還回去，所以反悔之後還能重裝）。
  換下來的技能冷卻不重置。線上對戰時不是自己的回合也能開背包整理。
- **一鍵裝備**：角色卡右上角的「一鍵」和背包上方的「全隊一鍵裝備」，
  會把背包裡分數最高的裝備和品質更高的技能自動換上（照 `PT_W` 那把尺算分，仍受每欄一次的限制）。
- **金寶箱**：除了王座四周固定四個，全圖也會鏡像散幾個；加了金色光柱和旋轉光環，遠遠就看得出來。
- **設定面板**（右上角齒輪）：音量、UI 大小、背包每行格數、動畫速度、特效等級、陰影、傷害數字、
  名牌顯示、威脅範圍、鏡頭速度、自動結束回合、結束確認、自動分解。存在 localStorage，換裝置就是另一份設定。

## 十、戰鬥節奏調整

實測發現同級對戰下 TTK（打死對方要幾下）從 0.3 下到 77 下都有，數值拉開之後戰鬥不是
「秒殺」就是「怎麼打都不痛不癢」，過程沒有張力。原因是傷害公式用線性扣減
`攻擊 − 防禦`，防禦一旦追上攻擊，傷害就會被砍到地板值 1；反過來防禦跟不上攻擊時，
每一下又是對方血量的一大截。

- **傷害公式改成減傷曲線**：`傷害 = 攻擊 × 18 ÷ (18 + 防禦)`。防禦永遠有效但永遠不會把
  傷害壓到 0，全等級的交手穩定在 3～5 下，不會隨等級被拉開成兩個極端。
  騎士的守護被動、`guard` 詞綴這類固定減傷，也從「最後扣一個固定值」改成先併入防禦
  再算比例，一發爆擊命中會跟著等比例減少，不會出現「大招被 −2 抵銷到看不出差別」的怪狀。
- **怪群等級上限拉高**：`MON_LV` 從 `{1:1, 2:3, 3:5}` 改成 `{1:1, 2:4, 3:7}`。
  英雄封頂 Lv10，怪群原本卡在 Lv5，後期怪物在數值上已經追不上英雄，變成純粹的經驗值。
- **驚動一隻，整營地一起圍上來**：原本是一隻一隻叫醒，玩家可以站在邊緣把一整群怪一隻隻
  挑掉，怪群完全不成群。現在攻擊任何一隻（含技能傷害，原本技能傷害甚至不會驚動怪物）
  或走近，同一個營地的怪會同時醒過來一起反應。脫戰距離也從 6 格拉到 8 格，避免一群怪
  才剛湊在一起就因為玩家後退一步全部各自放棄。
- **「殲滅敵軍」判定收緊**：這個win condition原本是「檢查兩邊還有沒有活著的英雄」，
  怪群整營地圍上來之後，很容易一次把某一隊三個人都打進倒下狀態、瞬間觸發這個判定 ——
  但倒下的英雄下一回合就會在營地站起來，被野外的魔物摸到團滅不該直接輸掉整場比賽。
  改成只有**敵方英雄親手完成最後一擊**才算數，被怪物打趴的話比賽照常繼續。
- 實測數據（AI 對戰，記錄每一次普攻／技能命中扣掉的血量佔目標上限的百分比）：
  英雄被擊中平均每下掉 45% 血量，八成落在 26～100% 區間（也就是 1～4 下），
  不再出現舊公式那種 0～10% 的「不痛不癢」或單次 100%+ 的「秒殺」極端值。
  骨王（唯一的首領怪）平均每下只掉 38% 血，需要多次交手才能打倒，不再是走過去點一下的裝飾品。

沒有動的部分（留給下一輪再評估）：怪物是不是該有威脅性的技能而不只是普攻、
兩隊在地圖上第一次碰面通常要 10～20 回合（雙方各自在自己半邊刷怪，中線缺乏拉力）、
以及低防禦的兵種（小兵／術士）目前多數情況還是一擊必殺，這符合「脆皮」的設計定位，
但如果實際玩起來覺得太脆，`data.js` 的 `MON` 表可以單獨拉高它們的 HP 或防禦。

## 十一、金幣與競技場

- **金幣**：打怪固定小範圍掉（怪群等級越高越多，1 級 2～4、3 級 6～10、首領 20～30），
  開寶箱也固定掉一點（普通 5～10、金寶箱 15～25）。用固定範圍而不是機率，
  商店和競技場才有穩定的經濟基礎可以規劃，不會因為手氣不好完全出不起價。
- **競技場**（`src/arena.js`）：每 `ARENA_INTERVAL`（5）回合結束，雙方所有還站著的英雄被
  強制傳送到固定的 16×16 小地圖，打「先拿到 `ARENA_WINS`（3）分」的三戰制。
  地形和增益地塊位置是**手刻固定佈局**，180° 對稱，不用亂數——競技場的公平比多樣性重要，
  而且省掉一整條「地圖生成亂數要不要跟戰鬥亂數分開」的同步風險。
  - 力量增幅（金色）：撿到 `addSt(u,u,{id:'atk',pct:0.3,turns:3})`。
  - 蓄力（紫色）：撿到設 `u.charged=true`，下一次**普通攻擊**（`strike()`，含反擊）
    傷害翻倍並清掉旗標，同時疊上兩回合的 30% 減攻擊。
  - 贏家 40 金、輸家 10 金；結束後所有參賽者回到主戰場原本的位置，
    **血量統一設成上限的一半**（不管競技場裡打成怎樣），這是進場的風險代價。
  - 主地圖用跟建立新對局完全相同的方式重建：把 `CHESTS[].opened`／`TRAPS` 存起來，
    離場時 `setSize` + `genMap(原本的種子)` 把地形、營地、寶箱**重新生成回一模一樣的樣子**
    （地圖生成用的 `rng` 跟戰鬥用的 `grng`是分開兩條種子，重新生成主地圖不會弄亂
    戰鬥亂數的序列），再把 `opened`/`TRAPS` 蓋回去，怪物完全沒被動過所以直接接續原狀態。
  - 兩边同時觸發：進出競技場靠雙方都在跑的同一份 `G.turn`/`alive()` 狀態算出來的條件
    （跟魔物階段一樣），不需要額外的網路訊息去同步「現在該進競技場了」這件事。

  **踩過的坑**：進競技場時 `G.units.forEach(removeView)` 把全場模型都拆了，包含還留在
  主地圖上凍結的怪物——但怪物的 `x`/`y` 還是舊地圖的座標，競技場地圖小，這些座標常常
  剛好落在新地圖範圍內，變成「憑空冒出來的鄰居」。`unitAt()`／`alive()`／AI 的目標搜尋
  只看座標不看「這單位現在算不算在場上」，於是路徑計算和選目標會把凍結的怪物當成
  真的站在那裡，輕則搶當攻擊目標算出 `undefined` 的地形資料整個爆炸，重則把它當成
  路障擋住路。修法是加一個 `u.paused` 旗標：進競技場時，凡是沒被傳送過去的單位全部標記
  暫停，`alive()`/`unitAt()` 這兩個核心空間查詢函式都排除暫停中的單位，離場時解除。
  同一類問題也出現在死亡淡出動畫：`onCam(u)` 只檢查座標距離，不檢查模型還在不在，
  凍結中的怪物如果被殘留的中毒／流血拖死，會想對一個已經被拆掉的模型播動畫而炸掉——
  `die()` 和競技場自己的 `arenaDie()` 現在都先確認 `u.view` 真的存在才播放。

## 十二、商店與背包回合

- **商店**（`src/shop.js`）：每 `SHOP_INTERVAL`（3）回合結束開一次，5 件商品**由低到高依序拍賣**，
  4 件照 `Q_TABLE[3]` 隨機、保底 1 件金色。拍賣**簡化成回合制**——原本設計是即時倒數搶標，
  但 P2P 對等連線沒有裁判角色去判定「誰先按到」，改成雙方輪流「加價」或「放棄」完全避開這個問題：
  出價後換對方决定，放棄的話——對方已出過價就直接得標，兩邊都還沒出價就流標。
  最低加價門檻跟著品質走（白 10、綠 20、藍 30、紫 40、金 50），可以自己輸入更高的數字。
  技能類商品**不分邊**，只要兩隊當中有人的職業學得會就會上架，得標才給那一隊，
  用 `giveBookId()` 給指定的那一本（跟掉落用的 `giveBook()` 不同，掉落是隨機重骰，
  商店要能先讓雙方看到「賣的是哪一本」，得標之後必須是同一本）。
  商店跟怪物階段**不衝突**——商店結束後照樣接著跑 `monsterPhase()`，
  不像競技場需要整個換地圖，商店不影響任何單位位置，沒有理由讓怪物停手。
  跟競技場撞期（`SHOP_INTERVAL` 和 `ARENA_INTERVAL` 的公倍數，也就是每 15 回合）時，
  `doEndTurn()` 的檢查順序是**競技場優先**，那個回合的商店就跳過。

- **背包回合**（`src/bagphase.js`，**只有線上對戰**才有）：原本「每個欄位一回合只能換一次」
  的限制，線上模式改成「每個人的回合結束後插入一段 60 秒背包回合，這段時間內雙方
  想換幾次都行，時間到或雙方都跳過就整批鎖住」。`canManage()` 在線上模式下從
  「這局還沒結束就好」收緊成「這局還沒結束**而且現在是背包回合**」，本機模式完全沒動。<br>
  倒數用 `Date.now()` 各自在本地算，不強求毫秒對齊——跟怪物階段一樣的思路：
  兩邊各自跑同一套邏輯，不需要一個外部時鐘去裁決。跳過用 `{t:'bagskip'}` 廣播給對方，
  雙方都跳過或任一邊本地計時器到了就結束，呼叫 `doEndTurn()` 原本該做的事
  （`proceed()` 這個延續函式——下一位的回合、或該檢查競技場／商店了）。<br>
  背包本身**隨時能打開看**（沒動這條），只有真正的裝卸／換技能會被 `canManage()` 擋下來，
  訊息會明確告知「要等背包回合才能整理」。

## 十三、石頭視線、競技場改單場制、UI 微調

- **石頭與視線**（`data.js` / `rules.js` / `ai.js`）：新增地形 `K`（石頭），`cost:99` 不可通行、
  `block:1` 標記會被視線判定吃到，沒有任何攻防加成——跟山地那種「地形也是資源」的設計
  分開，石頭純粹是掩體。視線用 `hasLOS(a,b)`／`canReach(a,b)`（`rules.js`）判斷：
  沿兩點連線密集取樣，只要有一個取樣點落在 `block` 地形上就算擋住；近戰距離 ≤1 直接略過
  這個檢查（貼臉打架不該被地圖上的石頭卡）。`targetsOf()`、`canCounter()`、`stopToHit()`、
  UI 點擊攻擊、AI 選目標的迴圈都補上了 `canReach()` 檢查——漏掉 AI 那邊會變成「玩家的
  弓箭手被石頭擋住打不到，電腦的弓箭手卻直接穿牆打你」，看起來像作弊。

- **競技場改回合單場制**（`arena.js`）：原本「先贏 3 場」的三戰制拿掉了，一次觸發只打一場
  打到某一方全滅為止，理由是每 5 回合觸發一次、三戰制等於一次進場最多要打 3 場，
  次數比預期的密很多。連帶拿掉 `ARENA_WINS`／`arenaRoundOver()`／`resetArenaRound()`，
  `arenaDie()` 判斷全滅後直接呼叫 `exitArena()`，不再有「這輪結束、重新滿血開下一輪」的分支。
  - **地形隨機化**：原本是手刻固定佈局（180° 對稱、圖固定），改成用
    `mulberry32((G.seed ^ (G.turn * 0x1000193)) >>> 0)` 當種子——跟主地圖生成的 `rng`、
    戰鬥用的 `grng` 都不同條，只用「主局種子 × 這次進場的回合數」算出來，雙方各自算
    結果保證一致，不用額外送網路訊息同步佈局。石頭跟地形變化（森林／山地／沼澤）
    分兩輪亂數放置：石頭先放、確保是真正的障礙物，接著另一輪才在剩下的平原格上
    隨機灑地形變化，兩者互不影響。
  - **四種增益地塊**：從 2 種（力量、蓄力）擴充到 4 種，新增守護（3 回合護盾）、
    迅捷（2 回合移動 +2），每次進場**保證四種都各出現一個**，用「最小間隔 3 格」
    的亂數擺放避免擠在一起。
  - **數值調整**：力量增幅攻擊加成 30%→20%，蓄力的下一擊加成 100%→50%
    （同時拿掉原本蓄力附帶的「用完後 −30% 攻擊 3 回合」懲罰，四選一了就不用
    再疊一個副作用增加決策負擔）。
  - **城堡蓋住單位**：`world.js` 的 `buildWorld()` 原本無條件在王座旁的固定座標
    放城堡跟旗幟模型，競技場的小地圖座標系統跟主地圖共用，剛好落在單位重疊的位置——
    改成 `if (!G.arena) {...}` 整段跳過，競技場本來就不需要這兩個純裝飾模型。

  **踩過的坑（鏡頭）**：`startTurn()` 換邊時會 `moveCam()`（不等它跑完，純視覺效果，
  不該卡住回合）把鏡頭帶到該隊單位的平均位置。如果紅方回合一開始就馬上結束
  （測試用腳本連續呼叫 `doEndTurn()` 最容易踩到，但玩家秒收工也有機會遇到），
  這個補間可能還在跑，接著競技場觸發、`enterArena()` 直接把鏡頭座標設定到新的小地圖上——
  但補間沒被作廢（`camJob` 沒被 bump），舊補間跑完之後又把鏡頭拉回舊地圖算出來的座標，
  離競技場的範圍老遠。修法是 `enterArena()`／`exitArena()` 換鏡頭前先 `camJob++`，
  跟 `moveCam()` 本來就有的「有新指令就讓位」機制接上。

- **UI 微調**：
  - 「輪到你」的提示原本統一顯示金色，現在跟著該隊自己的顏色走（藍方回合藍色、
    紅方回合紅色）——CSS 拿掉 `.mine` 的金色覆蓋，讓 `.s0`/`.s1` 的隊伍色顯示出來。
  - 商店開場沿用跟 `turnBanner()`／`arenaBanner()` 同一套「大字飛到上方」動畫
    （抽成共用的 `showBanner()`），金色字、`#turnBanner::before` 本來就是半透明黑色
    放射漸層，不會整個蓋住畫面。另外商店本身的操作面板（`#shopOv`）背景也從
    完全不透明的選單漸層換成半透明黑色漸層，拍賣的時候還看得到後面的棋盤。
  - 商店面板改兩欄：左邊商品資訊卡、右邊拍賣操作和自己的金幣。加價從純數字輸入
    改成 +/− 按鈕（每次 5 金幣）配合可以直接手動輸入的數字框。

## 十四、背包回合時機修正、AI 崩潰修正

- **背包回合插錯位置**（`rules.js`）：`doEndTurn()` 原本是「先判斷要不要插背包回合，
  裡面才判斷藍方剛結束還是紅方剛結束」，等於藍方一結束、紅方都還沒動，就先插一次
  背包回合，紅方結束又再插一次——變成每個人結束自己的回合都夾一段，而不是
  「雙方這輪都走完了」才夾一次。正確順序應該是**所有玩家回合 → 背包回合 → 魔物／
  競技場／商店**。修法是把「藍方剛結束就直接換紅方」這個分支移到背包回合判斷**之前**，
  背包回合只留在「紅方也結束了」這條路徑上，跟原本設計的頻率（一輪一次）對上。
  這個問題也連帶造成「玩家回合可以裝備」的觀感——`canManage()` 本來就只在
  `G.bagPhase` 為真時才放行管理動作，管理面板一直都只在真正的背包回合才能操作，
  只是背包回合插得太密、跟正常回合黏在一起，才會讓人感覺兩者界線不清楚。

- **AI 對手接管後直接卡死**（`ai.js`）：紅方的目標評分迴圈在算「這個候選落點打不打得到」
  時，`canReach(a, b)` 的第一個參數要拿去查 `dmgType(a)`（判斷 a 是不是近戰，決定要不要
  查視線），必須是一個真正的單位（有 `.cls`/`.side`）。但這裡誤傳了一個只有座標的
  `{x, y}` 空物件，`dmgType()` 內部的 `base(a)` 找不到職業資料就回傳 `undefined`，
  再讀一次 `.dmg` 直接丟例外，而且是在 `aiTurn()` 的迴圈裡沒被接住，
  整個 AI 回合當場卡死，紅方按了「AI 對手」也不會有任何動作。
  修法很簡單：這個迴圈裡 `u.x`/`u.y` 在算分前就已經被暫時改成候選落點了，
  直接傳 `u` 本身就好，不需要另外包一個座標物件。

## 十五、陷阱致死卡回合、技能鎖行動、競技場殘留倒數圈、連線 TURN

- **踩陷阱直接死亡會讓整個回合卡死**（`rules.js`）：`animMove()` 裡踩到敵方陷阱扣血，
  如果那一下直接扣死（傷害不低、或本來就殘血），`hurt()` 內部會呼叫 `die()`，
  而 `die()` 收尾時會 `removeView(u)` 把模型拆掉。問題是 `animMove()` 兩個分支
  （在鏡頭內/ 不在鏡頭內）都是「不管死沒死，`hurt()` 跑完就接著 `placeUnit(u)`」，
  對一個 `u.view` 已經是 `null` 的單位取 `.g.position` 直接丟例外。`runAction()`
  的 `busy = false` 是散落在各個分支結尾手動設的，不是包在 `finally` 裡，
  這個例外一丟出來，`busy` 就永遠卡在 `true`——`canAct()` 全域擋死，
  整場遊戲所有人都動不了，只能重新整理。修法是兩個分支的 `hurt()` 之後都補一句
  `if (!u.alive) return;`，跟同一個函式裡 `bleedOnMove()` 後面本來就有的寫法一致。

- **一騎當千（`u_solo`）用了等於白放**（`rules.js`）：這張技能的設計是「本回合殺人
  就恢復行動」（`killRefresh:9`），意思是要**放技能之後接著攻擊**才有意義。但
  `runAction()` 對所有 `kind:'skill'` 一律 `u.acted = true`，放完技能當下就已經
  不能行動了，「這回合殺人才有用」根本碰不到——其他 `buffSelf` 技能（鷹眼專注、
  秘法護盾、狂暴）buff 效果是留到之後幾回合用的，鎖行動沒問題，只有這張是例外。
  修法：`kind:'skill'` 分支改成檢查 `SK[a.skill].killRefresh`，有這個欄位的技能
  放完不鎖行動。

- **競技場飄著跟這場戰鬥無關的紅圈**（`arena.js` / `world.js`）：紅圈是
  `refreshRespawn()` 畫的「單位倒下復活倒數」，怪物是紅色。問題是：如果進競技場
  那一刻，主戰場剛好有魔物正在倒數重生，牠的倒數圈已經在畫面上了，`enterArena()`
  換了地圖、鏡頭、單位都重建，卻沒有再呼叫一次 `refreshRespawn()`——舊的圈圈用
  舊地圖的座標系統算出來的世界座標還留在場上，跟競技場的小地圖疊在一起，
  變成幾個憑空飄浮、對不上任何東西的圈圈。修法兩處：`enterArena()` 補呼叫
  `refreshRespawn()`；順便讓 `refreshRespawn()` 自己跳過 `u.paused` 的單位
  （凍結中的不用畫），這樣以後同類情況也不會再冒出來。

- **連線加了 TURN 中繼**（`net.js`）：追查「客人輸入完房號、房主能選角但客人進不去」
  這個回報時，本機兩個分頁走一次完整的建房→加入→雙方選角→開局流程完全正常，
  排除掉是選角/開局那段邏輯的問題。最大的嫌疑落在 WebRTC 本身：PeerJS 預設雲端
  信令伺服器只提供 STUN，兩邊都在對稱式 NAT 後面時（常見組合：一邊家用寬頻、
  一邊行動網路熱點），單靠 STUN 常常打不穿，會卡住不動也不一定觸發 `error` 事件，
  跟回報的症狀吻合。加了免費的公開 TURN（Open Relay Project）當中繼備援，
  直連失敗時能繞過去。這個沒辦法在單機環境重現／驗證是否真的解決，
  需要實際換兩台不同網路的裝置再測一次。

## 十六、爆發倍率下修、怪物等級與精英變種、TURN 設定修正

- **爆發倍率下修**（`data.js`）：背擊 ×1.5→×1.25、剋制三角 ×1.2（原 ×1.5）。
  這兩個是跟暴擊（×1.5）分開算再連乘的，抓到「背擊＋剋制＋暴擊」全中，
  三個一乘很容易把脆皮職業一次打穿，是「常常一擊必殺」的主要嫌疑之一。
  先調這兩個是因為它們最容易疊在同一下攻擊上；技能倍率、裝備成長速度那些
  之後有需要再回頭調。

- **怪物等級與精英變種**（`data.js` / `world.js` / `rules.js` / `inv.js`）：
  - `MON_LV` 加了第 4 級（Lv.10，跟英雄封頂一樣），對應 `placeCamps()` 新增
    一圈貼著王座本體的「最終守衛」，雙首領＋法師＋戰士壓陣。這圈只有
    48×48 以上的地圖才擠得下（半徑跟著地圖大小比例算，太小的地圖硬塞
    會跟王座本身的柱子疊在一起），48/64 兩個尺寸各是很花時間的長局，
    等玩家真的打到這一圈時角色也該練得差不多了，時機上算合拍。
  - 精英變種：`mkMon()` 生成時 15% 機率把一般怪（首領除外，首領本來就是
    尖兵）標記成精英，`statOf()` 讀到 `u.elite` 就把血量/攻擊/防禦各自
    乘上 `MON_ELITE` 表定的倍率（1.4 / 1.25 / 1.15）。外觀跟首領共用
    「換色＋放大」那套做法（`buildUnitView()`），只是換一個沒被用過的
    紫色調，一眼就跟隊伍色、首領的金色分得開。擊殺經驗、金幣、掉落機率
    也都對應加成，名字前面加「精英」兩個字（`nameOf()`），血條名牌會
    自動帶出來，沒有另外加 UI。
  - 這整套刻意不新增模型、不碰地圖生成邏輯（除了新增那一圈王座守衛的
    半徑計算），純粹是資料表 + 既有的「換色放大」手法的重組，換來的是
    「隨時可能遇到一隻不一樣的怪」的變化感，成本很低。

- **連線 TURN 設定其實是覆蓋掉了 PeerJS 自己內建的 TURN**（`net.js`）：上一輪
  加 `config: ICE_CONFIG` 想加一組公開 TURN 當備援，後來翻 PeerJS 原始碼才發現
  `new Peer(id, {config: ...})` 的 `config` 是**整個物件覆蓋**、不是合併——
  PeerJS 1.5.4 其實內建了自己的 TURN（`turn.peerjs.com`），沒把它一起抄進來，
  等於用一組來源不明的免費 TURN 換掉了官方維護的那組，可能反而更不穩。
  現在把 PeerJS 內建 TURN 跟額外加的 Open Relay TURN 都塞進同一個 `iceServers`
  列表，兩組誰能連就用誰。連線逾時的時間也從 15 秒拉到 25 秒——
  繞去 TURN 中繼本來就比 P2P 直連多花幾秒協商，15 秒對「本來還在忙、
  只是比較慢」的情況太容易提前判死刑。這兩個改動都沒辦法在單機環境
  驗證是否真的解決跨網路連線問題，需要實機測試。

## 十六、競技場凍結畫面、主地圖石頭、連線逾時分階段

- **進競技場後畫面凍結在原本的主地圖**（`ui.js`）：使用者回報「看得到血條
  名牌、小地圖也對、WASD 有反應，但 3D 畫面完全沒切過去」。追出來是
  `projectTags()`（每幀跑一次，`renderer.render()` 之前）會呼叫
  `drawRespawnTags()` 畫「怪物/英雄倒下重生倒數」的浮動數字，這個函式
  篩選單位時只看 `!u.alive && u.down > 0`，**沒有排除 `u.paused`**——
  進競技場時被留在主地圖上凍結的怪物，如果剛好死了在等重生，牠的
  `home`/`spawnAt` 座標是主地圖（例如 32×32）的座標，對競技場的小地圖
  （16×16）來說整排都出界，`ter(x,y)` 拿到 `undefined` 再讀 `.h`
  直接丟例外。這個例外每一幀都在 `renderer.render()` 之前發生，畫面
  就永遠停在丟例外前最後一次成功畫出來的那一幀（也就是離開的主地圖），
  但 `projectTags()` 裡排在 `drawRespawnTags()` 前面的血條名牌邏輯、
  以及 `panStep()`（WASD 移動鏡頭）都排在丟例外的地方之前，照樣正常
  運作，才會出現「名牌血條正常、小地圖正常、WASD 有感覺，畫面卻沒換」
  這種看起來互相矛盾的組合。這其實跟前一版修過的
  「競技場殘留倒下復活紅圈」是同一種病（`refreshRespawn()` 那次也是
  沒排除 `u.paused`），只是這次是姊妹函式 `drawRespawnTags()`
  （畫在 DOM 上的數字標籤，跟畫 3D 紅圈的 `refreshRespawn()` 是分開兩支
  程式碼）沒有一起修到。順便把負責建立/回收這批 DOM 標籤的
  `refreshRespawnTags()` 也加上同一個排除條件，兩邊算出來的清單筆數才會
  對得上。

- **主地圖也加了石頭障礙**（`world.js`）：之前石頭只有競技場在用，
  主地圖維持用山地/森林當地形變化，完全沒有真正的「不可通行」障礙物。
  新增 `placeRocks()`，邏輯跟競技場那套幾乎一樣（一叢一叢放、偶爾黏一格、
  180° 鏡像對稱、只放在還是平原的格子上、避開營地周圍），差別是用地圖
  生成自己的 `rng`（不是戰鬥用的 `grng`），叢數依地圖面積算
  （`W*H/120`，最少 3 叢）。連帶把 `placeCamps()`／`placeChests()`
  的地形排除清單也加上 `'K'`，怪物營地和寶箱都不會生成在石頭上——
  怪物個體會不會站到石頭上则完全不用額外處理，`freeNear()` 本來就會跳過
  `cost > 90` 的地形。

- **連線逾時訊息分兩階段**（`net.js`）：原本逾時只有一句「連線逾時」，
  沒辦法從訊息本身判斷是卡在哪一步。改成追蹤「自己的 Peer 有沒有連上
  PeerJS 雲端信令伺服器」這個旗標，逾時時分開講：階段 1（信令都連不上，
  通常是網路本身擋掉對外連線，跟 WebRTC/TURN 無關）、階段 2（信令沒問題、
  雙方都找得到對方，但 P2P／TURN 中繼交握卡住，這一步才是 STUN/TURN、
  對稱式 NAT 在起作用）。純粹是診斷用的訊息調整，不確定能不能真的解決
  使用者回報的「跨網路還是連不上」——這個問題到目前為止還沒辦法在單機
  環境重現，只能靠更精確的錯誤訊息讓下次回報時能講出卡在哪一階段。

## 十七、遊俠弩弓素材錯誤、AI 自己管裝備、地圖/怪物數值大改、山地換素材、競技場轉場動畫

- **遊俠的弩弓「看不見」，其實是模型本身的旋轉值下錯了**（`main.js`）：
  查了很久——視野判定、材質、幾何、骨骼綁定、frustum culling 全部正常，
  拿同一個 geometry/material 另外生一個 mesh 掛到同一根手部骨骼上也能
  正常顯示。最後直接比對全部職業/階段的武器旋轉值才找到：劍、斧、杖…
  全部都是 `[π,0,-π]`，唯獨遊俠的「1H_Crossbow」是 `[0,π/2,0]`，
  角度完全不對，弩弓因此卡進手臂/身體裡，從遊戲常用的等角視角看起來
  就是「武器不見了」，只有投影出來的影子還看得出形狀，跟回報的症狀
  完全對得上。修法很輕量：在模型載入完成後，直接在共用的角色範本上
  把這一個 mesh 的旋轉改回跟其他武器一致的 `[π,0,-π]`，不用動
  `buildUnitView()` 的邏輯，所有複製體（含頭像預覽）自動沿用修正。
  法師的帽子、狂戰士的斧頭實測旋轉值都跟這個「正確」的模式一致，
  目前找不到對應的顯示異常，先當作沒問題處理。

- **AI 接管的一方會自己管裝備和屬性點**（`ai.js`）：電腦控制的隊伍原本
  屬性點、裝備都放著不動，因為沒有玩家會去點背包。新增 `aiManageSelf()`，
  在 `aiTurn()` 開始行動之前先跑一次，屬性點攻擊/生命/防禦輪流分一輪
  （不是精算最優解，求的是「至少不要放著浪費」），裝備直接呼叫玩家
  本來就有的「一鍵裝備」（`autoGearAll()`）。

- **地圖大小改成 16/32/64/96/128，怪物等級和數量都跟著地圖大小走**
  （`data.js` / `world.js`）：原本 `MON_LV` 是固定表，現在改成
  `monLvFor(tier)`，四個 tier 各佔「這個地圖大小的最高等級」的固定比例
  （10%/40%/70%/100%），最高等級本身依地圖大小查表（16→7、32→10、
  64→15、96→22、128→30）。營地圈數也改成依地圖大小查表
  （`CAMP_RING_N`），四圈由外而內，數字是照怪物總數大概湊出來的
  （16→32、32→64、64→128、96→192、128→256），不是精算，數字有落在
  使用者要的範圍附近但不是每個都剛好命中。

- **山地跟石頭障礙用同一顆模型，長得一模一樣分不出來**（`world.js`）：
  兩個地形之前都疊 `rock_c`，改成山地用完全沒被用過的第三種素材
  `rock_a`（角度比較尖、比較大顆），配一兩棵樹裝飾成「有樹的主峰」，
  石頭障礙維持原本「矮胖一叢好幾顆、光禿禿」的樣子，輪廓差夠多，
  一眼看得出哪個能走哪個不能走。

- **進競技場加了一整套轉場動畫**（`index.html` / `style.css` /
  `arena.js`）：原本進競技場跟平常換邊回合共用同一套「大字飛到面板」
  的動畫，太普通。新增獨立的 `#arenaFx` 覆蓋層：灰階模糊淡入、字從小
  放大到滿（0.7 秒）→ 字用 CSS `animation` 左右搖晃像船一樣，維持到
  第 5 秒 → 灰色背景用 `radial-gradient` 配一個可以動畫的 `--r` 自訂
  屬性，從外圈往中心收（2 秒，效果是「畫面邊緣先恢復、中間最後才恢復」）
  → 退場退到一半（1 秒整）時字改用 CSS `transition` 滑出畫面左邊並淡出。
  踩到一個坑：中間補間階段用 inline style 設定 `transform`/`opacity`，
  切到搖晃/滑出這兩個用 class 控制動畫的階段時忘記把 inline 樣式清掉——
  `transition` 類的 CSS（不像 `animation`）不會自動蓋過 inline 樣式，
  導致滑出效果完全沒作用、字卡在原地不動。修法是補間結束、要交給
  class 動畫接手之前，把 inline 的 `transform`/`opacity` 清空。

## 十八、怪物數量再調高、營地放置加重試機制、競技場轉場時間調整

- **64/96/128 的怪物數量再往上調**（`world.js`）：目標從 128/192/256
  改成 256/576/1024，數字本身比上一輪大了不少。第一次直接把
  `CAMP_RING_N` 的營地數量按比例往上乘，結果 128 那格算出來的怪物數
  遠低於預期（795 隻，跟目標 1024 差不少）——密度越高，`placeCamps()`
  角度算出來的落點越容易撞到別的營地或不能放的地形，原本的邏輯是
  「這個落點放不下就直接放棄、不重試」，越密集這個放棄率就爬得越高，
  單純往上加營地數量的邊際效益很差。修法是幫 `placeCamps()` 加重試：
  同一個角度放不下就沿著**同一個角度**、半徑往外或往內挪個幾格再試
  幾次（半徑微調不會破壞左右鏡射對稱，隨機亂跳位置才會），加上這個
  之後三個尺寸的營地幾乎 100% 放得下，這時候才把 `CAMP_RING_N` 調回
  接近精算值（64→258、96→576、128→1026，都很接近目標）。營地之間的
  最小間距也順便從 3 格放寬到 2 格，密集擺放時更好塞。

- **競技場轉場動畫調時間軸**（`arena.js` / `style.css`）：灰色背景維持
  時間從 5 秒改成 3 秒；字消失的時機原本是「背景退場退到一半（1 秒）
  時才開始滑出」，改成跟背景**同時開始、同時結束**——`.leaving` 的
  CSS `transition` 時長從 1 秒改成 2 秒（跟背景退場的補間一樣長），
  觸發時機也從「延遲 1 秒後才加 class」改成跟背景退場補間同一刻
  一起加，兩個動畫用同一個時間軸跑完，不會有字先消失、背景還在收尾
  的落差感。
