# 專案狀態：待辦、進行中、遇到的問題

> 這份檔案只記「現在該做什麼、正在做什麼、卡在哪裡」。
> 所有數值、公式、設計細節都搬去 [info.md](info.md) 了，這裡不重複貼數字，
> 需要看細節就點連結過去查。

## 進行中：裝備改浮動點數分配

**特效詞綴 AFFIX 那半已經做完發布了**（見下面「已修掉」列表），這裡
只剩「浮動點數分配」還沒動。設計已經跟使用者來回定案好幾輪，**細節在
[info.md](info.md)「裝備（浮動點數分配，設計中）」節**。

- **還沒寫任何程式碼**，純設計階段。
- 還有一個沒問過使用者的小缺口：防具扣掉保底之後剩下的點數怎麼分配，
  武器已經明講「留一部分機率浪費掉」，防具還沒問是不是也要套同一套
  邏輯，還是單純均分——標在 info.md 該節的 ⚠️ 待確認。
- 動工時要動的檔案清單也寫在 info.md 對應章節的最後（`GEAR` 資料結構、
  `rollItem()`／`itemStats()`／`itemName()`）。
- 開工前流程：重讀 info.md 那節 → 補問那個防具的缺口 → 寫程式 →
  `node build.js` 語法檢查 → 瀏覽器實測掉落幾把裝備確認數值範圍合理
  → 更新 `skills.md` 改版紀錄 → `update.ps1` 發布。

## 未解決的 BUG

1. **競技場移動單位時，畫面上會突然從玩家沒看到的地方跳到目的地**——
   使用者回報過，**還沒修**。調查到一半的假設：`buildUnitView()`
   （`src/world.js`）建立新模型時**沒有先移除舊的模型**（直接
   `unitGroup.add(g)`，沒檢查 `u.view` 是不是已經存在），懷疑進競技場時
   英雄的舊模型（主地圖上的）沒被清掉，變成一個沒人在追蹤座標的幽靈
   殘影，玩家看到的可能是殘影的位置，實際邏輯位置是另一個——這只是
   假設，還沒證實也還沒修。
   下一步：先在瀏覽器 console 數 `unitGroup.children.length` 進競技場
   前後有沒有異常增加來證實，證實後在 `buildUnitView()` 開頭加一行
   「已經有舊 view 就先 `removeView(u)`」。
   （這個排查跟下面「競技場畫面凍結」那個已修的 bug 是不同成因——
   凍結是渲染整個停住，這個是模型位置對不上，兩個要分開看。）
2. ~~**`drawRespawnTags()` 偶爾丟 `ter(...).h` undefined 例外**~~——
   **已修（技能改版第二批一起解掉）**：倒下的英雄常常 `spawnAt` 和
   `home` 都沒有（`home` 只有怪物在 `mkMon` 設），`u.spawnAt || u.home`
   是 `undefined`，`s[0]` 先炸（`reading '0'`），出界座標再往下就是
   `ter(...).h`（`reading 'h'`）。`drawRespawnTags()` 裡 `s` 退回
   `CAMP[u.side]`，再不行就 `display:none` 跳過。新版 CC 讓英雄倒得
   更頻繁，才把這個洞從偶發變成每幀刷、順勢定位到。`task_23d889df`
   可以關了。

## 這一批已經修掉、發布過的東西（給後續對話快速對照，不是待辦）

之前有一版把幾個已修的 BUG 誤列成「還沒查」，這裡更正：

- **AI 對手接管後直接卡死**（commit `dead392`）：紅方選目標評分時
  `canReach()` 誤傳了一個只有座標的 `{x,y}` 空物件（該傳單位本身），
  `dmgType()` 內部讀不到職業資料丟例外，例外沒被接住，`aiTurn()`
  當場卡死，紅方按 AI 對手完全沒反應。已修：改傳單位本身 `u`。
- **背包回合插錯位置**（commit `dead392`）：原本每個玩家結束自己的
  回合都會插一次背包回合，改成只有「雙方這輪都行動完」才插一次
  （所有玩家回合 → 背包回合 → 魔物／競技場／商店）。這個問題同時也是
  「玩家回合感覺可以裝備」的成因——`canManage()` 本來就只在背包回合
  才放行，只是背包回合插太密、跟正常回合黏在一起才會有這種錯覺，
  時機修好後這個感受應該會一起消失（沒有另外找使用者覆核過）。
- **踩陷阱直接死亡會讓整個回合卡死**（commit `da1da25`）：`animMove()`
  兩個分支踩到陷阱死亡後沒有提前 return，對已經被拆掉模型
  （`u.view === null`）的單位取位置直接丟例外，`busy` 卡在 `true`
  永遠出不來，全場所有人都動不了。已修：兩個分支補上
  `if (!u.alive) return;`。
- **一騎當千（`u_solo`）用了等於白放**（commit `da1da25`）：這張技能
  設計是「本回合殺人就恢復行動」，但放技能當下就已經被鎖住不能行動，
  「殺人恢復行動」根本碰不到。已修：`runAction()` 對有 `killRefresh`
  欄位的技能不鎖行動。
- **競技場飄著跟這場戰鬥無關的紅圈**（commit `da1da25`）：進競技場前
  如果主戰場剛好有魔物在倒數重生，那個紅圈沒有跟著清掉/重算，變成
  用舊地圖座標畫在新的小地圖上、對不上任何東西的飄浮圈圈。已修：
  `enterArena()` 補呼叫 `refreshRespawn()`，順便讓它跳過凍結中
  （`u.paused`）的單位。
- **石頭視線阻擋、競技場改單場制＋地形隨機＋四種增益、城堡蓋單位
  修正、回合色改隊伍色、商店大字提示與雙欄 UI、鏡頭補間競技場錯位
  修正**（commit `90ce519`）。
- **背擊/剋制三角倍率下修、怪物 Lv4 王座最終守衛與精英變種**
  （commit `3173311`）。
- **進競技場後畫面凍結在原本的主地圖**（commit `d5ae9d7`）：
  `drawRespawnTags()`（`ui.js`，每幀跑一次）畫怪物/英雄倒下重生的浮動
  數字時沒排除 `u.paused` 的單位，凍結在主地圖上的怪物如果剛好在倒數
  重生，牠的座標對競技場的小地圖來說整排出界，`ter(x,y).h` 直接丟
  例外——例外發生在 `renderer.render()` 之前，畫面就凍結在丟例外前
  最後一幀（也就是離開的主地圖），但排在丟例外前面的血條名牌、WASD
  鏡頭移動都還在正常運作，才會出現「名牌血條正常、小地圖正常、WASD
  有感覺，3D 畫面卻沒切過去」這種矛盾組合。跟前一版修的「競技場殘留
  紅圈」（`refreshRespawn()`）是同一種病，只是這次是姊妹函式
  `drawRespawnTags()` 沒有一起修到。
- **主地圖新增石頭障礙**（commit `d5ae9d7`）：之前石頭只有競技場在用，
  現在 `placeRocks()`（`world.js`）也會在主地圖灑幾叢，`placeCamps()`／
  `placeChests()` 也排除了石頭格，怪物/寶箱不會生成在石頭上
  （`freeNear()` 本來就會跳過不可通行地形，不用額外處理）。
- **遊俠弩弓顯示不出來**（commit `dfc9e30`）：素材本身的問題——
  遊俠「1H_Crossbow」的旋轉值 `[0,π/2,0]` 跟其他所有武器（劍/斧/杖…
  全部 `[π,0,-π]`）不一致，弩弓因此卡進手臂裡，從常用視角看起來像
  完全不見了。已在模型載入後（`main.js`）直接修正共用範本的旋轉值。
  法師的帽子、狂戰士的斧頭實測旋轉值正常，找不到對應的顯示異常，
  詳細除錯過程寫在 `skills.md` 十七節。
- **AI 接管會自己管裝備和屬性點**（commit `dfc9e30`）：新增
  `aiManageSelf()`（`ai.js`），`aiTurn()` 開始行動前先跑，屬性點攻擊/
  生命/防禦輪流分、裝備呼叫玩家原有的「一鍵裝備」。
- **地圖大小改成 16/32/64/96/128，怪物等級與數量跟著地圖大小走、
  山地換了不會跟石頭障礙撞樣子的模型、競技場進場加了完整轉場動畫**
  （commit `dfc9e30`，細節都在 [info.md](info.md) 對應章節，這裡不重複）。
- **怪物營地生成邏輯，經過三輪才定案**：一開始把 `CAMP_RING_N`（固定
  半徑的圈）乘大想湊使用者要的數量，密度太高常撞在一起被放棄
  （commit `c886b75` 加了重試機制想補救，但重試會讓營地擠成一圈一圈
  同心圓，使用者回報「生成位置變成一個一個圈了」）。先改回沒有重試的
  簡單版本（commit `e21e4a3`），但問題根源其實是「固定半徑＋固定角度」
  這個演算法本身就是在畫圓，不是重試機制的問題，改回簡單版還是有
  同心圓感。**最後跟使用者討論了幾個方案（隨機抖動角度/半徑、網格
  抖動、帕松盤採樣），選定網格抖動（尚未發布）**：把地圖切成一格一格
  抖動撒點，等級看候選點離王座的距離落在哪個級距，不再有任何固定
  半徑或角度，數量用格子邊長逐一調過，5 個地圖大小都落在合理範圍。
  細節在 [info.md](info.md)「營地等級」章節。
- **單位視錐外直接不渲染（效能優化）**（commit `e21e4a3`）：地圖跟怪物
  數量都變大很多，使用者回報「更卡、幀率變低」。原本 `animate()` 只有
  「鏡頭範圍外的單位不更新骨骼動畫」，現在同一個距離判斷也拿來設
  `u.view.g.visible`，範圍外的單位整個跳過渲染流程（選中/滑鼠指到的
  單位不受限制）。邏輯用手動計算核對過沒問題，但**這個自動化瀏覽器
  測試環境的 `requestAnimationFrame` 不會像真人分頁那樣持續 60fps 跑**
  （只有截圖/滑鼠事件之類的動作才會推進一格），沒辦法在這裡測出真實
  幀率有沒有改善，需要使用者在正常瀏覽器分頁裡實測回報。
- **競技場轉場動畫「文字一出現幾乎立刻消失、完全看不到搖晃」，這次
  找到真正的 bug 了**（commit `4286d2b`）：上一輪使用者回報「還是 5 秒」時，
  懷疑是瀏覽器快取，但這輪使用者給出更具體的症狀（字幾乎立刻消失、
  搖晃動畫完全看不到）才抓到真正原因——`#arenaFxTxt` 的基礎 CSS 規則
  本來就寫死 `opacity:0`（給進場動畫開始前那一瞬間用的預設值）。進場
  補間跑完後程式碼會清空 inline 的 `opacity`，換 `.grown` 這個 class
  接手搖晃動畫，但 `.grown` 只用 `animation` 控制 `transform`、完全
  沒設 `opacity`，inline 一清空就打回基礎規則的 `opacity:0`，文字瞬間
  不可見，搖晃動畫全程跑在透明度 0 的元素上，等於整段「維持」時間
  完全看不到任何東西。已經幫 `.grown` 補上 `opacity:1` 修掉。上一輪
  完全沒抓到這個問題，純讀程式碼看不出破綻，是直接拿瀏覽器跑一次
  `.grown` 狀態讀 `getComputedStyle` 才發現的——這也代表使用者先前
  回報的「還是 5 秒／太早消失」很可能一直都是這個 bug，不是快取問題，
  之前歸咎快取是誤判。
- **怪物等級改成連續漸層（離王座距離線性遞減，不再四圈跳看）＋競技場
  轉場動畫退場「瞬間變小消失」的 bug**（本次發布）：
  `monLvFor` 從 tier 查表改成吃 `frac`（營地離王座距離比例）直接算，
  `frac` 從 `placeCamps()` 一路存進 `CAMPS`／傳進 `mkMon()`；實測 128
  地圖一局出現 24 種等級（3～29）、32 地圖 5 種（2～7），不再是固定的
  3/12/21/30 四個值。轉場動畫退場的 bug 跟上面 opacity 那個是同一個
  成因模式——基礎規則除了 `opacity:0` 還寫死 `transform:scale(.3)`，
  `.grown`→`.leaving` 交接時打回這個縮小值，變成「先跳小、才開始滑走」
  看起來像瞬間消失。這次直接把兩個預設值都從基礎規則拿掉（`arenaBanner()`
  一開始就會用 inline style 設好，基礎規則留著只是地雷）。細節見
  [skills.md](skills.md) 二十一節、[info.md](info.md) 的「營地等級」
  「競技場」章節。
  順便發現一個**跟這次改動無關**的既有問題，已經另開背景任務追查
  （task_23d889df）：全新開一局本機對戰，開局第一回合 console 就噴了
  幾十次 `drawRespawnTags()` 的 `ter(...).h` 例外（`ui.js`），事後查
  `G.units` 裡符合條件的清單是空的，沒抓到觸發源頭。

- **裝備詞綴改成按品質分級、技能介紹卡住擋畫面的 bug**（本次發布）：
  使用者發現常見的吸血裝備跟金裝效果一樣強（詞綴數值本來全部寫死，
  不分品質），照 info.md 早就定案的分級表把 6 個詞綴的數值改成按品質
  （常見/稀有/史詩/傳說）查表，`first`／`swift` 常見稀有查不到（沿用
  原本的部位限制設計）。技能介紹卡住的 bug 是技能列常常在滑鼠沒動的
  情況下整批重建按鈕，導致 `pointerenter`/`pointerleave` 漏接，改成
  每 400ms 檢查目前介紹卡片綁定的按鈕是不是真的還在被滑鼠壓著
  （`el.matches(':hover')`），比使用者建議的 2 秒逾時更即時，也天生
  能抓到「按鈕被砍掉重建」這個根本成因。細節見 [skills.md](skills.md)
  二十二節、[info.md](info.md)「特效詞綴 AFFIX」節。

- **技能改版第一批：騎士全套照 `newskill.md` 改、5 個新狀態機制、
  牧師普攻治療隊友減半**（本次發布）：使用者拿了一份 `newskill.md`
  （已收進 repo 根目錄 [newskill.md](newskill.md)），方向是「品質＝多重效果／控場，不是傷害
  倍率更大」。討論後拍板騎士＋法師照稿改、硬控先照稿必定觸發、分兩批。
  這批做了共用引擎（新狀態 `taunt`/`disarm`/`slow`/`guarded`/`mitig`/
  `immune`/`rip`、統一的 `takeDmg()` 出入口、`frontbox` k 型、
  `single`/`teleport` 補 `self`、`zocCost`/`retalFlat` 被動、`shield`
  的 `hpPct`、`lastStandCheck` 改給無敵）＋騎士 15 技全部重寫（沿用
  舊 id），另外把牧師普攻治療隊友（`runAction` 的 `heal` 分支，非技能）
  砍半。**法師是下一批，還沒動。** 細節見 [skills.md](skills.md) 二十三節、
  [info.md](info.md)「技能」節。

## 已出但沒辦法在單機驗證的修正

- **跨網路連線逾時，現在確定卡在哪一階段了**：commit `d5ae9d7` 把逾時
  訊息拆成階段 1（連不上信令伺服器）/ 階段 2（信令沒問題但 P2P/TURN
  交握卡住）。**使用者這輪回報是階段 2**——signaling 沒問題，兩邊都
  找得到對方，但 P2P／TURN 中繼一直建立不起來。目前已經上了兩組 TURN
  （PeerJS 內建 + Open Relay Project 免費公開服務），還是卡住，最大
  嫌疑是**免費 TURN 服務本身不夠穩定**（常見的已知限制：流量/連線數
  被限速、伺服器不穩），不是設定寫錯。下一步如果還要繼續查，比較實際
  的方向是換一組更穩定的 TURN（通常要花錢，例如自架 coturn 或用
  Twilio/Metered.ca 的付費方案），免費方案很難再往上優化太多——
  這點已經在聊天裡跟使用者說明過，等使用者決定要不要往這個方向投入
  再繼續。這個問題沒辦法在單機環境重現，只能靠使用者回報繼續縮小範圍。

## 停在「文本討論」階段、還沒有具體行動的方向

這些是使用者提出來討論、明確說「先不要動」或還沒拍板要不要做的，
列在這裡避免忘記，**不要自己主動開工**，等使用者明確說要開始再動：

- **技能大規模重做——五職業全部改完發布**：定案方向在
  [newskill.md](newskill.md)。第一批（騎士＋共用引擎）、第二批
  （狂戰士／遊俠／法師／牧師＋持續地塊 `field`／隨機多目標 `rand`）
  都上了，細節見 [skills.md](skills.md) 二十三、二十四節。**還沒動的
  只剩通用 15 技**（`u_*`），使用者沒特別要求，先擺著。硬控目前照稿
  「必定觸發」，使用者玩過覺得太壓制的話再改成機率觸發（`newskill.md`
  也有提這個備案）。
- **天賦（C～S 級）系統**：使用者自己提的點子（例：狂戰士 A 級「普攻
  20% 觸發連擊」），**明確說「技能還沒定好，這些先不做」**——純粹
  記錄構想，等技能方向確定再回來討論。
- **牧師接近必備、其他職業辨識度不足**：使用者回報的平衡感受。已經先
  動一刀——牧師普攻治療隊友減半（技能治療不影響）。技能改版鋪完之後
  再看還缺不缺別的手段。
